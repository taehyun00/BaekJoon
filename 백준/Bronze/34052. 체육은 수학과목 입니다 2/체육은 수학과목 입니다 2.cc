// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int a,b,c,d;
   scanf("%d %d %d %d",&a,&b,&c,&d);
   
   (a+b+c+d <= 1500) ? printf("Yes") : printf("No");

    return 0;
}