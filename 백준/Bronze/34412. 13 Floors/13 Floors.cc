// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num;
    scanf("%d",&num);
    if(num < 13 ) printf("%d",num);
    else printf("%d",num+1);

    return 0;
}