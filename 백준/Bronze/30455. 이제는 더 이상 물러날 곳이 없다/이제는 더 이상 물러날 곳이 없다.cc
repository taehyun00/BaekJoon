// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num;
    scanf("%d",&num);
    if(num%2 == 1 ){
        printf("Goose");
        return 0;
    } 
    else printf("Duck");

    return 0;
}