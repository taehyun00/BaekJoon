// Online C compiler to run C program online
#include <stdio.h>

int main() {
    long long int num;
    scanf("%lld",&num);
    if (num%5==0 || num%5==2){
    printf("CY");
    return 0;
    }
    else printf("SK");

    return 0;
}


